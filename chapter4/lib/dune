(menhir
 (modules parser)
 (flags ("--dump" "--explain")))

(ocamllex
 (modules lexer))

(library
 (name ch4)
 (libraries core stdio)
 (preprocess (pps ppx_deriving.show)))

(env (dev (flags (:standard -warn-error -A))))
