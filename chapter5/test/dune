(executable
 (name testsuite)
 (libraries core ch5 alcotest)
 (preprocess (pps ppx_deriving.make)))

(alias
 (name runtest)
 (action (run ./testsuite.exe)))

(env (dev (flags (:standard -warn-error -A))))
