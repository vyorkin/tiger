let
  function main(n : int) =
    for i := 0 to n do
      let
        function nested(x : int) =
          if x > 5 then
            break
       in
         nested(i)
       end
 in
  main(10)
end
