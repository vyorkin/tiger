let
  function main(n : int) =
    for i := 0 to n do
      let
        function nested(x : int) =
          if x > (n + i) then
            break
       in
         nested(i)
       end
 in
  main(10)
end
