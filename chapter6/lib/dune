(menhir
 (modules parser)
 (flags ("--dump" "--explain")))

(ocamllex
 (modules lexer))

(library
 (name ch6)
 (libraries core stdio)
 (preprocess (pps ppx_deriving.show ppx_compare)))

(env (dev (flags (:standard -warn-error -A))))
